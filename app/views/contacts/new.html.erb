<h2><i class="fas fa-envelope mr-3"></i>お問い合わせ</h2>
<hr />
<p>下記フォームよりお問い合わせください。</p>

<div class="d-flex justify-content-center">
  <div class="col-7 mt-5">
    <%= form_with(model: @contact, local: true) do |f| %>
      <% if @contact.errors.any? %>
        <div class="alert alert-danger alert-dismissible">
          <h2><%= @contact.errors.count %> つのエラーがあります。</h2>
    
          <ul>
          <% @contact.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
    
      <div class="form-group">
        <%= f.label :title %>
        <%= f.select :title, Contact.titles_i18n.invert, {}, class: "form-control" %>
      </div>
    
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: "form-control" %>
      </div>
    
      <div class="form-group">
        <%= f.label :email %>
        <%= f.text_field :email, class: "form-control" %>
      </div>
    
      <div class="form-group">
        <%= f.label :content %>
        <%= f.text_area :content, class: "form-control", rows: 7 %>
      </div>
    
      <%= f.submit "送信する", class: "btn btn-success text-white btn-lg btn-block mt-5" %>
    <% end %>
  </div>
</div>
